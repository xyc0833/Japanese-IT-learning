# software的基础内容

### ソフトウェアの詳細（软件的详细分类）

| 日文分类 | 日文原文说明 | 中文翻译说明 |
| :--- | :--- | :--- |
| **基本ソフトウェア** | Operating System（OS）の略でコンピューターの基本的な動作を担う<br>（カーネル） | 操作系统（OS）的简称，负责计算机的基础运行<br>（核心部分：内核） |
| **ミドルウェア** | 多くのアプリケーションが共通して必要とするが、OSでは提供出来ないような機能を持つソフトウェア | 中间件：提供很多应用程序共同需要、但操作系统本身不具备的功能的软件 |
| **応用ソフトウェア** | 私たちが普段使っているアプリケーション（Excel・Word・メールソフトなど） | 应用软件：我们日常使用的应用程序（如Excel、Word、邮件软件等） |

---

### 💡 一句话总结
- **基本软件（OS/内核）**：管电脑底层的基础运行，比如开机、内存管理、硬件交互。
- **中间件**：夹在OS和应用软件之间，给多个应用提供通用功能（比如数据库连接、消息队列）。
- **应用软件**：我们直接用来干活的程序，比如办公软件、聊天工具。

カーネル  Kernel


#  GUI 和 API

## 1. GUI（Graphical User Interface）
### 翻译与读音
- 日语原文：**GUI（ジーユーアイ）**
- 英文全称：**Graphical User Interface**
- 中文：**图形用户界面**

### 核心定义（结合图中说明）
图里的描述是：
> 視覚的に操作して命令を伝える画面
> （通过视觉操作来传递命令的界面）

简单来说，就是我们每天用电脑、手机时看到的：
- 窗口、图标、按钮、菜单
- 鼠标点击、触屏滑动这些操作方式

它把复杂的计算机指令，变成了我们能看懂、能直接点的“图形界面”，让普通人也能轻松用电脑，不用背命令行指令。

**例子**：Windows 的桌面、macOS 的 Dock、手机上的App图标，全都是 GUI。

---

## 2. API（Application Programming Interface）
### 翻译与读音
- 日语原文：**API（エーピーアイ）**
- 英文全称：**Application Programming Interface**
- 中文：**应用程序编程接口**

### 核心定义（结合图中说明）
图里的描述是：
> アプリケーションからOSが保持している各種機能を利用するための仕組み
> （应用程序用来调用操作系统所提供的各种功能的机制）

通俗解释：
API 就是**程序和程序之间沟通的“接口/约定”**。
比如你写了一个应用程序（応用ソフトウェア），它想让操作系统（基本ソフトウェア/OS）帮它做一件事（比如读取文件、显示窗口、播放声音），它不能直接去碰系统底层，而是要通过 API 来“请求”系统提供这些功能。

**例子**：
- 你写一个App，想在屏幕上显示文字，不用自己写底层代码，只要调用操作系统提供的 `DrawText` API 就行。
- 手机App调用相机、定位功能，也是通过系统的 API 实现的。

---

## 两者的核心区别（帮你一眼分清）
| 维度 | GUI | API |
| :--- | :--- | :--- |
| **使用者** | 人（用户） | 程序/开发者 |
| **交互方式** | 鼠标点击、触屏、可视化操作 | 代码调用、函数/指令 |
| **目的** | 让用户能直观地操作电脑 | 让程序能安全、便捷地调用系统/其他程序的功能 |
| **例子** | 电脑桌面、微信界面 | 程序里调用的 `open()` 函数、地图App调用定位接口 |

---

💡 补充小知识：
- 它们都属于操作系统的“对外接口”，只是服务的对象不同：**GUI 给人用，API 给程序用**。
- 你平时用电脑，是通过 GUI 跟系统交互；而你电脑上的各种软件，是通过 API 跟系统交互的。

### ① ジョブ/タスク管理
CPUをどのように使用するか管理すること
OSが上手くCPUをコントロールすることで、複数処理を同時進行で行っているように見える

**翻译：**
① 作业/任务管理
管理CPU的使用方式
操作系统通过高效地控制CPU，让多个处理看起来像是在同时进行

---

### ② 記憶管理
主記憶装置の容量は少ないので上手く記憶領域を管理する必要

### ③ ファイル管理
ファイルやフォルダの管理もOSが行っている

**翻译：**
② 内存管理
由于主存储器（内存）的容量有限，需要对存储区域进行高效管理

③ 文件管理
文件和文件夹的管理也由操作系统负责

# スプーリング（spooling）
- **读音**：スプーリング（supu-ringu）
- **英文全称**：Simultaneous Peripheral Operations On-Line
- **中文**：假脱机技术/缓冲输出技术

## 核心概念
这是一种操作系统的**输入输出优化技术**，最典型的应用场景就是打印任务：
1.  当你在电脑上点击打印，数据不会直接发给打印机，而是先被临时存到硬盘/内存的缓冲区（这个过程就是“假脱机”）。
2.  打印机空闲时，再从缓冲区按顺序读取数据进行打印。
3.  这样一来，你的电脑就不用等打印机慢吞吞地打完，能立刻继续做别的事，而打印机也能处理多个任务，实现“并行工作”。

## 核心作用
- 让**低速外设（如打印机）**和**高速CPU**的速度差被缓冲掉，避免CPU空等。
- 实现多个任务的“排队处理”，看起来就像多个设备在同时工作，大幅提升系统效率。

# スループット 吞吐量、处理能力
### 图片内容翻译
**原文：**
単位時間当たりに処理できる仕事量（スループット）が向上

**翻译：**
单位时间内能够处理的工作量（吞吐量/Throughput）得到提升

### スループット（Throughput） 介绍
- **读音**：スループット（suru-putto）
- **英文**：Throughput
- **中文**：吞吐量、处理能力

#### 核心含义
在计算机领域，**スループット**指的是系统、设备或程序在**单位时间内能够完成的处理量/工作量**，是衡量系统整体处理效率的关键指标。
- 例：
  - CPU：每秒执行的指令数
  - 网络：每秒传输的数据量
  - 打印系统：每分钟完成的打印任务数

#### 补充说明
你之前看到的「スプーリング（假脱机）」技术，核心目的之一就是**提升系统的スループット**：通过缓冲低速外设（如打印机）的处理，让CPU不用等待，从而在单位时间内完成更多任务，也就是让“单位时间的工作量（スループット）”变高。

「割り当てる」（わりあてる /wariateru）是日语动词，核心含义是分配、分派、指派


ディスパッチャ（Dispatcher）
读音：ディスパッチャ（disupaccha）
英文：Dispatcher
中文：调度程序 / 分派器

原文	翻译	核心含义
ディスパッチャ ：CPU の割当を実行するプログラム	执行 CPU 分配的程序	

ディスパッチ ：CPU を割当てることを	（负责）分配 CPU 的操作 / 行为	给进程分配 CPU 资源的动作 / 功能


# 3つのスケジューリング（三种调度方式）

① 到着順方式（到達順序方式 / FCFS） 
> ノンプリエンプティブ方式
原文：実行可能状態になった順番にCPUの使用権を割り当てる
翻译：按任务进入就绪状态的先后顺序，分配CPU使用权

② 優先順方式（优先级方式 / Priority Scheduling）
> プリエンプティブ方式
原文：優先順位が高いタスクから順にCPUの使用権を割り当てる
翻译：按任务优先级从高到低的顺序，分配CPU使用权

③ ラウンドロビン方式（轮转调度 / Round Robin）
> プリエンプティブ方式
原文：一定時間ごとにCPU使用権を切り替える
翻译：每隔固定时间，切换一次CPU使用权

# プリエンプティブ方式（Preemptive / 抢占式调度）
**原文**：OSがCPUを管理し、実行中のタスクを途中で中断して他のタスクにCPU使用権を割当てる
**翻译**：由操作系统管理CPU，中途打断正在执行的任务，将CPU使用权分配给其他任务

# ノンプリエンプティブ方式（Non-Preemptive / 非抢占式调度）
**原文**：実行中のタスクを途中で中断せずプログラムに処理を任せる
**翻译**：不中途打断正在执行的任务，将处理完全交由程序自行完成

# 割込み処理（Interrupt Processing / 中断处理）
这是操作系统中一种打断当前执行、优先处理紧急事件的机制，分为两类：

## ① 内部割込み（内部中断）
- **日文原文**：内部割込み（プログラムが要因）
- **中文翻译**：内部中断（由程序自身原因引发）
- **说明**：由正在执行的程序内部触发的中断，比如除零错误、内存访问越界、系统调用等程序运行异常或主动请求。

## ② 外部割込み（外部中断）
- **日文原文**：外部割込み（プログラム外の要因）
- **中文翻译**：外部中断（由程序外部因素引发）
- **说明**：由CPU外部设备触发的中断，比如键盘输入、鼠标点击、硬盘读写完成、定时器信号等硬件事件。

💡 一句话总结：
**割込み処理**就是让CPU能响应“紧急事件”的机制，内部中断来自程序自身，外部中断来自外部设备。

わりこむ（warikomu）
词性：自动词（五段动词）
核心译法：插入、打断、插嘴、插队；中断（IT 场景）
英文对应：interrupt, cut in, break in

我来帮你把这张「内部中断（内部割込み）」的表格拆解清楚，包含术语、翻译和通俗解释：


# 内部中断的两种类型
这张表介绍了**内部割込み（内部中断）**的两种主要类型，都是由程序运行过程中自身行为引发的中断，用来让CPU暂停当前任务、处理特殊情况。

## 二、两种内部中断详解

### 1. プログラム割込み（程序中断 / 异常）
- **日语原文**：プログラム割込み
- **中文**：程序中断（也叫“异常”）
- **英文**：Program Interrupt / Exception
- **发生原因**：
  处理することが出来ない不正な命令により発生
  （由程序执行了CPU无法处理的非法/异常指令引发）

- **例子**：
  - `0での割り算`：除零错误
  - `オーバーフロー`：数值溢出（计算结果超出数据类型的表示范围）
  - `ページフォールト`：缺页中断（程序访问的内存页面不在物理内存中，需要从硬盘加载）

- **通俗理解**：程序自己“闯了祸”，CPU处理不了，只能停下来找操作系统帮忙解决。

### 2. SVC割込み（SVC中断 / 系统调用中断）
- **日语原文**：SVC割込み
- **中文**：SVC中断（系统调用中断）
- **英文**：SVC Interrupt（Supervisor Call Interrupt）
- **发生原因**：
  プログラムがOSに入出力要求したとき等に発生
  （当程序向操作系统发起输入/输出请求等操作时触发）

- **通俗理解**：程序需要操作系统帮忙做“它自己做不了的事”，比如读写文件、访问硬件，就通过SVC指令主动触发中断，让CPU切换到操作系统模式来处理请求。

## 三、两者核心区别
| 类型 | 触发方式 | 性质 | 目的 |
|------|----------|------|------|
| プログラム割込み | 程序执行异常指令（如除零、溢出） | 被动触发的“错误/异常” | 处理程序运行中的故障，保证系统稳定 |
| SVC割込み | 程序主动调用SVC指令 | 主动触发的“请求” | 让程序安全调用操作系统提供的服务 |

💡 补充小知识：
- SVC 全称是 **Supervisor Call**（管理程序调用），它是用户态程序进入内核态的标准方式之一，也是操作系统实现资源管理的关键机制。
- 你可以把プログラム割込み理解为“程序出问题了喊救命”，把SVC割込み理解为“程序找OS帮忙办事”。


# 外部割込み（外部中断）
外部中断是由**CPU外部的硬件/事件**触发的中断，用来处理硬件相关的紧急情况。下面是表中4种类型的中日对照说明：

| 日文术语 | 中文翻译 | 发生原因（原文+翻译） |
| :--- | :--- | :--- |
| 機械チェック割込み | 机器检查中断 | 原文：電源の異常や装置の故障等、ハードウェアの不具合で発生<br>翻译：因电源异常、设备故障等硬件问题而触发 |
| タイマ割込み | 定时器中断 | 原文：設定された時間が経過してプログラムが中断されることで発生<br>翻译：设定的时间到达后，程序被中断而触发 |
| コンソール割込み | 控制台中断 | 原文：PCを操作している利用者の介入が行われた際に発生<br>翻译：用户操作电脑、进行干预时触发（如键盘输入） |
| 入出力割込み | 输入输出（I/O）中断 | 原文：入出力装置の動作完了時や中断時に発生<br>翻译：输入输出设备（如硬盘、打印机）完成操作或发生中断时触发 |

💡 补充说明：
- 这些外部中断，正是你之前学的「プリエンプティブ方式（抢占式调度）」和「スプーリング（假脱机）」技术的基础。
  - タイマ割込み：让操作系统能定时打断程序，实现多任务轮转（Round Robin）。
  - 入出力割込み：让CPU不用空等低速设备，设备完成操作后主动通知CPU，大幅提升效率。


  动词「見かける」	① 看见、看到（偶然 / 不经意间）
② 看起来、看上去	街で彼を見かけた。（在街上看到了他。）
彼は若く見かける。（他看起来很年轻。）
名词「見かけ」	外表、外观、表面	見かけに騙される。（被外表欺骗。）


# 实记忆管理（実記憶管理）
这是操作系统管理**主存储器（内存）**的核心机制，下面按术语、翻译、说明帮你整理清楚：

## 1. プログラム読み込み方式（程序加载方式）
### ① 固定区画方式（固定分区方式 / Fixed Partitioning）
- **日文原文**：固定区画方式
- **中文翻译**：固定分区方式
- **说明**：把内存预先划分成固定大小的区域，程序只能加载到匹配大小的分区里。
  - 优点：处理速度快，实现简单
  - 缺点：容易产生**无駄領域（碎片/浪费空间）**，内存利用率低，无法加载超过分区大小的程序（如图中12MB的程序无法加载到4MB分区）

### ② 可変区画方式（可变分区方式 / Variable Partitioning）
- **日文原文**：可変区画方式
- **中文翻译**：可变分区方式
- **说明**：根据程序的实际大小，动态划分内存分区，减少空间浪费，但容易产生外部碎片，需要额外的内存整理操作。

---

## 2. フラグメンテーション（Fragmentation / 内存碎片）
- **日文原文**：フラグメンテーション
- **中文翻译**：内存碎片
- **说明**：内存被零散分割，出现大量无法被有效利用的小空间，分为两种：
  - 内部碎片：分区内未被使用的剩余空间（固定分区常见）
  - 外部碎片：分区之间的零散空闲空间（可变分区常见）

---

## 3. メモリコンパクション（Memory Compaction / 内存压缩/整理）
- **日文原文**：メモリコンパクション
- **中文翻译**：内存压缩/内存整理
- **说明**：
  > 主記憶装置をギュッとする：メモリコンパクション
  > ハードディスクをギュッとする：デフラグ（デフラグメンテーション）
  - **内存压缩（メモリコンパクション）**：移动内存中的程序，把零散的空闲空间合并成连续的大块，解决外部碎片问题。
  - **磁盘碎片整理（デフラグ）**：整理硬盘上的文件碎片，让文件数据连续存储，提升读写速度。

---

💡 一句话总结：
固定/可变分区是程序加载的两种方式，而内存碎片是分区管理带来的副作用，需要通过内存压缩来解决；硬盘的碎片整理则是针对外部存储的同类优化操作。


# オーバーレイ方式（Overlay方式 / 覆盖技术）
- **日文原文**：オーバーレイ方式：主記憶の容量以上のプログラムを実行する際に使用
- **中文翻译**：覆盖方式：用于执行大小超过主存容量的程序

### 核心说明
- **日文原文**：1つのプログラムをセグメントと呼ばれる独立した処理単位に分割し、必要な部分だけを使用
- **中文翻译**：将一个程序分割为名为「段（セグメント）」的独立处理单元，只在主存中加载并使用当前需要的部分

💡 补充说明：
覆盖技术是早期解决**“程序大小超过物理内存容量”**的一种方法：
1.  把程序拆分成多个独立的“段”（セグメント）；
2.  运行时只把当前需要执行的段加载到主存；
3.  后续需要的段会覆盖掉不再使用的旧段，从而让大程序能在有限内存中运行。
它是虚拟内存出现之前的内存管理技术，现在已被分页/分段机制取代。


# スワッピング方式（Swapping / 交换技术）
- **日文原文**：スワッピング方式
- **中文翻译**：交换方式（也叫对换技术）

### 两个核心流程翻译
| 日文原文 | 中文翻译 |
| :--- | :--- |
| 中断したプログラムを主記憶装置から補助記憶装置に一時退避<br>スワップアウト | 将中断的程序从主存临时保存到辅助存储设备（硬盘）<br>**换出（Swap Out）** |


| 処理を再実行する際に主記憶装置に呼び戻す<br>スワップイン | 当需要再次执行该程序时，将其从辅助存储设备调回主存<br>**换入（Swap In）** |

スワップアウト 

スワップイン
---

### 补充说明
- **主記憶装置**：主存储器（内存）
- **補助記憶装置**：辅助存储设备（硬盘等外部存储）
- 作用：当主存空间不足时，把暂时不用的程序“换出”到硬盘腾出空间，需要时再“换入”，让更多程序能同时运行，是虚拟内存的基础机制之一。

---
💡 一句话理解：
スワッピング方式就是**程序在内存和硬盘之间“换进换出”**的机制，解决主存容量不足的问题。

# メモリリーク 内存泄漏（Memory Leak）

## 术语翻译与核心说明
- **メモリリーク**：内存泄漏（Memory Leak）
- **主記憶装置**：主存储器（内存）
- **ガベージコレクション**：垃圾回收（Garbage Collection）

##  图示总结
1.  **现象**：原本程序（命令A/B/C/D）使用的内存区域，因OS漏洞或程序错误，在处理结束后没有被正常释放，变成无法使用的区域。
2.  **后果**：内存中出现越来越多的“无效占用”，可用空间持续减少。
3.  **解决方式**：通过垃圾回收机制，回收这些未释放的内存区域，使其重新可用。

# 仮想記憶（虚拟内存）的基本机制

### 核心概念
- **仮想記憶領域（虚拟内存空间）**：给程序提供的一个“逻辑上的、巨大的连续内存空间”，这个空间是虚拟的，不是物理上真实存在的。
- **主記憶装置（物理内存/RAM）**：实际的内存条，容量有限。
- **補助記憶装置（辅助存储/硬盘）**：用来存放程序暂时不用的部分，比如交换分区/页面文件。

### 图中机制说明
1.  程序并不是一次性全部加载到物理内存里，而是先被映射到**虚拟内存空间**中。
2.  程序运行时，只有当前需要执行的部分，才会被实际加载到物理内存；暂时不用的部分，会被放在硬盘里。
3.  程序看到的是一个完整、连续的“虚拟内存空间”，感觉自己独占了全部内存；但操作系统会在背后，把它的内容分散放在物理内存和硬盘里，并自动完成两者之间的交换。

### 一句话理解
虚拟内存就是：**让程序“感觉自己有很大的连续内存”，而实际上物理内存和硬盘会配合，按需加载数据，解决物理内存容量不足的问题**。

💡 补充一下和你之前学的知识点的关系：
- 虚拟内存正是通过**分页/分段**+**交换（Swapping）**技术实现的。
- 它解决了早期覆盖技术（オーバーレイ方式）需要程序员手动管理内存的麻烦，完全由操作系统自动处理。


# 動的アドレス変換機構（DAT, Dynamic Address Translation）

动态地址转换机构

二、核心作用
它的核心任务是：把程序使用的「虚拟地址」，动态地转换成物理内存的「实际地址」，也就是我们之前说的「仮想アドレスと物理アドレスの紐付け（虚拟地址与物理地址的绑定 / 映射）」。
程序只能看到连续、巨大的虚拟内存空间
操作系统 / 硬件通过 DAT，在程序运行时实时完成地址转换，让程序数据可以分散存储在物理内存和硬盘中
对程序来说，整个过程是完全透明的，它根本不知道自己在用虚拟内存
三、工作原理（结合你学过的知识点）
动态转换：每次程序访问内存时，CPU 都会自动触发 DAT 机制，用页表 / 段表把虚拟地址转换成物理地址。
按需加载：如果数据不在物理内存中（发生缺页），DAT 会配合交换（Swapping）机制，把硬盘里的数据换入内存再进行访问。
保护隔离：DAT 还会在转换时检查地址权限，防止程序越界访问其他进程的内存，提升系统安全性。
四、一句话总结
DAT（动态地址转换）就是虚拟内存的 “翻译官”，它在程序和物理内存之间搭了一座桥，让程序以为自己独占了巨大的连续内存，同时又解决了物理内存容量不足的问题。


# 分页方式

### 简单介绍
**分页方式（ページング方式）**下的**缺页中断（ページフォールト）**机制，是虚拟内存管理的核心流程之一。

简单来说：
程序访问虚拟内存时，如果对应的页面不在物理内存里，就会触发缺页中断，操作系统会把硬盘里的页面加载到内存，再让程序继续运行。

---

### 原文翻译对照
| 日文原文 | 中文翻译 |
| :--- | :--- |
| ページング方式 | 分页方式（虚拟内存的分页管理机制） |
| CPU | CPU |
| 主記憶装置 | 主存储器（物理内存） |
| 補助記憶装置 | 辅助存储设备（硬盘） |
| エラー（読み込み不可） | 错误（无法读取） |
| 主記憶に存在しない | 不存在于主存中 |
| **ページフォールト** | **缺页中断（Page Fault）** |
| 仮想記憶領域にプログラムが読み込まれたが、その裏側で補助記憶装置にプログラムが眠ったままになっている | 程序被加载到了虚拟内存空间，但对应的程序数据实际还“沉睡”在辅助存储设备中 |

---

### 补充流程说明
1.  CPU访问程序的虚拟地址。
2.  地址转换时发现，对应的页面**不在物理内存中**，触发「ページフォールト（缺页中断）」。
3.  操作系统响应中断，从硬盘（補助記憶装置）中读取该页面。
4.  将页面加载到物理内存后，CPU重新执行访问指令，程序正常运行。

---
💡 小提示：这就是虚拟内存“按需加载”的具体实现，你之前学的「スワッピング（交换）」就是配合这个流程工作的。

## ページイン / ページアウト

ページイン / ページアウトが多発すると処理効率が低下（スラッシング）	
当页调入 / 页调出频繁发生时，处理效率会下降（抖动 / 颠簸）
ページイン時に主記憶装置の容量が一杯である場合、補助記憶装置に不要なページを退避	
页调入时，如果主存储器容量已满，则将不需要的页面退避到辅助存储设备
ページアウト	
页调出


# ページング置き換えアルゴリズム 分页置换算法

### 1. **FIFO (First In First Out)**
- **日文原文**：一番最初にページインしたページを追い出す
- **中文**：先进先出算法
- **核心逻辑**：谁最早被加载进内存，就把谁最先淘汰掉。
- **特点**：实现最简单，只需要记录页面的加载顺序。但缺点也很明显，可能会淘汰掉还在频繁使用的“老页面”，导致缺页率升高。

### 2. **LIFO (Last In First Out)**
- **日文原文**：一番最後にページインしたページを追い出す
- **中文**：后进先出算法
- **核心逻辑**：谁最后被加载进内存，就把谁最先淘汰掉。
- **特点**：与FIFO相反，淘汰最新的页面。这种算法在实际操作系统中很少使用，因为它不符合程序的访问规律，可能会导致频繁的缺页中断。

### 3. **LRU (Least Recently Used)**
- **日文原文**：最も長い時間参照されていないページを追い出す
- **中文**：最近最少使用算法
- **核心逻辑**：淘汰掉**最久没有被访问过**的页面。
- **特点**：它利用了程序的“局部性原理”——很久没被访问的页面，大概率以后也不会再用了。因此它是实际系统中最常用、效率最高的算法之一。

### 4. **LFU (Least Frequently Used)**
- **日文原文**：最も参照回数の少ないページを追い出す
- **中文**：最少使用频率算法
- **核心逻辑**：淘汰掉**被访问次数最少**的页面。
- **特点**：它关注的是页面的总访问频率，而不是上次访问的时间。缺点是，一些早期被频繁访问但现在已不再使用的页面，可能会一直留在内存里，占用空间。

# 重要なディレクトリ名（重要的目录名称）

- **カレントディレクトリ：今いるディレクトリ** current
  翻译：当前目录：你当前所在的目录

- **親ディレクトリ：カレントディレクトリの1個上**
  翻译：父目录：当前目录的上一级目录

- **サブディレクトリ：ディレクトリの中にあるディレクトリ**
  翻译：子目录：位于某个目录内部的目录

- **ルートディレクトリ：階層構造の一番上に位置するディレクトリ**
  翻译：根目录：位于目录层级结构最顶端的目录

- **ホームディレクトリ：ログイン直後に位置するディレクトリ**
  翻译：主目录/家目录：用户登录系统后，默认所在的目录

# マルチメディア 多媒体

### 一、汉字读音标注（按出现顺序）
- **マルチメディア**：无汉字，是「multimedia」的片假名
- **圧縮（あっしゅく / asshuku）**
- **解凍（かいとう / kaitō）**：这里的「解凍」在多媒体语境里是“解压缩”的意思
- **容量（ようりょう / yōryō）**
- **可逆（かぎゃく / kagyaku）**
- **不可逆（ふかぎゃく / fukagyaku）**
- **方式（ほうしき / hōshiki）**
- **復元（ふくげん / fukugen）**

---

### 二、图示与术语解释
#### 1. 核心流程
- **圧縮（压缩）**：将原本体积较大的音视频/图像数据，通过算法处理，缩小文件容量。
  图中右侧的 JPG、PNG、GIF（图像）、MOV、AVI、MP4（视频）都是常见的压缩格式。
- **解凍（解压缩/伸長）**：把压缩后的文件恢复成原始数据（或可播放/查看的状态）。

#### 2. 压缩方式分类
- **可逆圧縮方式（无损压缩）**
  「圧縮データを完全に復元できる」
  意思是：压缩和解压缩过程中，数据没有任何丢失，解压后能和原始文件一模一样。
  常见例子：PNG、GIF、ZIP、FLAC（音频）等。

- **不可逆圧縮方式（有损压缩）**
  「圧縮データを完全に復元できない」
  意思是：压缩时会丢弃部分数据（比如人眼/耳朵不敏感的细节），解压后无法完全恢复到原始状态，但文件体积会大幅减小。
  常见例子：JPG、MP3、MP4、AVI 等。

---

💡 补充说明：
- 图像/视频里，JPG 是典型的有损压缩，PNG 是典型的无损压缩；
- 视频格式（MP4、MOV、AVI）本身是“容器格式”，里面可以封装不同的压缩编码（比如 H.264、H.265），这些编码大多是有损压缩。

# 文件类型

### 静止画（せいしが）
1.  **BMP**
    - 日文：画像が圧縮されないため容量が大きい
    - 说明：无压缩的位图格式，文件体积大，画质完全无损。

2.  **GIF**
    - 日文：256色の可逆圧縮
    - 说明：最多支持256色的**无损压缩**格式，支持简单动画和透明背景。

3.  **PNG**
    - 日文：256色・もしくはフルカラーの可逆圧縮
    - 说明：支持256色或全彩色的**无损压缩**格式，画质比GIF好，支持透明背景。

4.  **JPEG**
    - 日文：フルカラーの不可逆圧縮。国際標準規格
    - 说明：支持全彩色的**有损压缩**格式，是国际标准，文件体积小，适合照片存储。

### 動画（どうが）
1.  **MPEG**
    - 日文：不可逆圧縮。国際標準規格
    - 说明：采用**有损压缩**的国际标准视频格式，是早期视频压缩的通用标准。


# 画像处理技术

我帮你把每个技术的**日文、英文全称、中文翻译**都对应补上：

---

1.  **アンチエイリアシング（Anti-Aliasing, AA）**
    - 日文原文：画像内に発生するギザギザなど、傾いた直線を滑らかにする技術
    - 中文：抗锯齿技术
    - 英文：Anti-Aliasing (AA)
    核心含义：消除图像边缘的锯齿感，让斜线和轮廓变得平滑自然

2.  **テクスチャマッピング（Texture Mapping）**
    - 日文原文：面像に柄や模様などを貼り付け、質感や立体感を表現する技術
    - 中文：纹理映射（贴图）技术
    - 英文：Texture Mapping
    给 3D 模型表面贴上图案 / 纹理，表现出材质的质感和立体感。

3.  **モーフィング（Morphing）**
    - 日文原文：ある物体画像変化の中間をコンピューターで演算し、変化の過程を表現する技術
    - 中文：变形（渐变/过渡）技术
    - 英文：Morphing
    通过计算机计算两张图像之间的中间状态，生成平滑的变形过渡动画。

4.  **レイトレーシング（Ray Tracing）**
    - 日文原文：画像内の水たまりなどの光の反射をよりリアルに表現する技術
    - 中文：光线追踪技术
    - 英文：Ray Tracing
    模拟真实光线的反射、折射效果，让水面、金属等材质的光影表现更逼真。

5.  **クリッピング（Clipping）**
    - 日文原文：ある領域からはみ出した画像を切り捨てる作業
    - 中文：裁剪（剪切）技术
    - 英文：Clipping
    只保留视野范围内的图像部分，把超出的部分裁剪掉，优化渲染效率。


6.  **シェーディング（Shading）**
    - 日文原文：物体画像に影を入れて、より立体感を出す技術
    - 中文：着色（光影处理）技术
    - 英文：Shading
  通过给物体添加明暗、阴影效果，表现出物体的立体感和光影质感。
































#  做题笔记


「競合」读作：きょうごう（kyougou）
I/O は競合せず	I/O wa kyousou sezu	I/O 设备不发生竞争

OS のオーバーヘッドは考慮しない	OS no oobaaheddo wa kouryo shinai	不考虑操作系统开销

「オーバーヘッド」（overhead）
“额外的负担 / 附加成本 / 额外开销”

常见几种意思：
IT / 计算机里（很常见）
指“额外消耗的资源”，比如时间、内存、CPU

我帮你把题目和选项里的日语都翻译清楚，顺便解释每个传感器的用途，你就知道答案了👇

---

## 一、题目翻译
原文：
> 変形を感知するセンサを用いると、高架道路などの状態を監視してメンテナンスすることが可能である。この目的で使用されているセンサはどれか。

翻译：
> 使用能感知**变形**的传感器，可以对高架道路等设施的状态进行监测和维护。下列哪种传感器是用于这个目的的？

---

## 二、选项翻译+详细解释
| 选项 | 日语原文 | 片假名/汉字 | 中文名称 | 英文原名 | 用途说明 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| ア | サーミスタ | サーミスタ | 热敏电阻 | **Thermistor** | 专门用来测量**温度**变化的传感器，和变形无关。 |
| イ | ジャイロ | ジャイロ | 陀螺仪 | **Gyroscope** | 测量**角速度/角度**的传感器，用于姿态、方向控制（比如手机、无人机），不直接测变形。 |
| ウ | ひずみゲージ | 歪みゲージ | 应变片/应变计 | **Strain Gauge** | 专门用来测量物体的**微小变形/应变**的传感器，是桥梁、道路、建筑结构监测的常用设备，和题目场景完全匹配。 |
| エ | ホール素子 | ホール素子 | 霍尔元件 | **Hall Element** | 利用霍尔效应测量**磁场**的传感器，可用于检测电流、位置、转速，和变形无关。 |

---

## 三、结论
✅ 正确答案是 **C（ウ：ひずみゲージ/应变片）**，因为它是唯一能感知变形、用于桥梁/道路结构监测的传感器。

---

💡 补充小知识：
应变片（ひずみゲージ）的原理是：当物体发生微小变形时，贴在上面的应变片电阻会发生变化，通过测量电阻就能算出变形量，从而判断结构是否出现异常。

素子 そし (soshi)，也常读为 もとこ (motoko)

変形（へんけい /henkei）
词性：名词・サ変动词（可以直接作动词：変形する）
含义：物体形状发生改变、变形

我帮你把这段内容整理成**日文原文 + 中文对照**的形式，方便你边看原文边理解👇

---

### センサとは、赤外線，磁気，音，温度，圧力，重さなどの物理的量を機械や人が認識できる電気信号や数値などの情報に変換する機器である。物を介したインターネットサービスIoT（Internet of Things）では、目的に応じた様々なセンサが利用される。
**翻译**：传感器是一种设备，它能将红外线、磁力、声音、温度、压力、重量等物理量，转换为机器或人类可识别的电信号、数值等信息。在以万物互联为基础的互联网服务IoT（物联网）中，会根据不同用途使用各种各样的传感器。

---

### 変形を感知するセンサはひずみゲージである。したがって、正解は（ウ）である。ひずみゲージは、対象物の変形を電気抵抗の値に変えて観測するセンサである。金属が圧力や張力、すなわち押し縮められたり引っ張られたりすることによって電気抵抗が変化する特徴を用いて、観測対象物に接着させておくと、抵抗値の変化を観測することによって変形度合いを測定することができる。例えば、身近な使用例では、荷重計に利用されている。
**翻译**：感知变形的传感器是应变片（ひずみゲージ）。因此，正确答案是（ウ）。应变片是一种将被测物体的变形转换为电阻值来进行观测的传感器。它利用金属在压力或张力（即被压缩或拉伸）作用下，电阻会发生变化的特性，将应变片贴在被测物体上，通过观测电阻值的变化，就能测量出变形的程度。例如，在日常生活中，它也被用于称重传感器（荷重计）。

---

### ア：サーミスタ……温度変化を電気抵抗の変化に変える機器である。エアコンや自動車等のエンジン温度の管理など様々な用途に利用されている。
**翻译**：ア：热敏电阻（サーミスタ）……是一种将温度变化转换为电阻变化的设备。它被广泛用于空调、汽车发动机温度管理等多种场景。

### イ：ジャイロ……回転する速さを検知する機器であり、スマートフォンやデジタルカメラの手振れ補正、カーナビゲーションシステムの自律走行機能（トンネル内など）などのために搭載されている。
**翻译**：イ：陀螺仪（ジャイロ）……是一种检测旋转速度的设备，被搭载在智能手机、数码相机中用于防抖，也用于车载导航系统的隧道内自主导航等功能。

### エ：ホール素子……磁気を検出するセンサである。電流に対して垂直に磁界をかけると電流、磁界のそれぞれに垂直な方向に電圧が発生すること（ホール効果）を利用する。エンジンの点火時期把握やモータの回転位置検知などにも用いられる。
**翻译**：エ：霍尔元件（ホール素子）……是一种检测磁场的传感器。它利用的是霍尔效应：当给电流施加垂直的磁场时，会在同时垂直于电流和磁场的方向上产生电压。它也被用于发动机点火时机控制、电机旋转位置检测等场景。


我来帮你把题目和选项完整翻译，顺便解释清楚每个概念👇

---

## 一、题目翻译
**インタプリタの説明として、適切なものはどれか。**
→ 问：下列哪一项是对「解释器（インタプリタ / Interpreter）」的正确说明？

---

## 二、选项翻译与解析
| 选项 | 日语原文 | 中文翻译 | 解析 |
| :--- | :--- | :--- | :--- |
| ア | 原始プログラムを、解釈しながら実行するプログラムである。 | 一边解释源程序，一边执行的程序。 | ✅ **正确**。这正是解释器的核心定义：不提前把整个程序翻译成机器码，而是边读边解释边执行（比如Python、JavaScript的解释器）。 |
| イ | 原始プログラムを、推論しながら翻訳するプログラムである。 | 一边推论，一边翻译源程序的程序。 | ❌ 错误。“推論（推论）”和翻译、执行程序无关，不属于解释器或编译器的定义。 |
| ウ | 原始プログラムを、目的プログラムに翻訳するプログラムである。 | 将源程序翻译成目标程序的程序。 | ❌ 错误。这是**编译器（コンパイラ / Compiler）**的定义：一次性把整个源程序翻译成机器可执行的目标程序，再执行，不是边解释边执行。 |
| エ | 実行可能なプログラムを、主記憶装置にロードするプログラムである。 | 将可执行程序加载到主存储器中的程序。 | ❌ 错误。这是**加载器（ローダ / Loader）**的功能，和解释器无关。 |

---

## 三、补充：解释器 vs 编译器 核心区别
| 类型 | 工作方式 | 典型例子 |
| :--- | :--- | :--- |
| 解释器（インタプリタ） | 边解释边执行，不生成独立目标文件 | Python、JavaScript、Ruby |
| 编译器（コンパイラ） | 先一次性翻译为机器码，生成目标程序后再执行 | C、C++、Java（先编译为字节码，再由JVM执行） |

---

✅ 正确答案：**A（ア）**


**编译器（コンパイラ / Compiler）**的定义：一次性把整个源程序翻译成机器可执行的目标程序，再执行，不是边解释边执行。


トランスレータ	toransurēta	翻译器	Translator


我帮你把这段内容翻译成**中日对照版**，方便你边看原文边理解👇

---

インタプリタは原始プログラムを1文（一命令）ずつ解釈しながら実行するプログラムで、かつて多くのパソコンなどに標準装備されていたBASICが代表的である。説明として適切なものは（ア）である。
**翻译**：解释器（インタプリタ）是一种一边逐句（逐命令）解释源程序，一边执行的程序，过去很多个人电脑上标配的BASIC语言就是典型代表。因此，正确的说明是选项（ア）。

---

また、COBOLなどのコンパイラ言語は、コンパイラを用いて原始プログラムを目的プログラムに翻訳し、リンカで複数の目的プログラムと組み合わせて実行可能なプログラムを作成する。それをローダが主記憶装置にロードしてプログラムを実行する。
**翻译**：此外，COBOL等编译型语言，会先通过编译器（コンパイラ）将源程序翻译成目标程序，再通过链接器（リンカ）将多个目标程序组合成可执行程序。最后，加载器（ローダ）会将这个可执行程序加载到主存储器中执行。

---

イ：原始プログラムを別の言語に翻訳するトランスレータなどである。
**翻译**：イ：是将源程序翻译成其他语言的翻译器（トランスレータ）等。

ウ：コンパイラである。
**翻译**：ウ：是编译器（コンパイラ）。

エ：ローダである。
**翻译**：エ：是加载器（ローダ）。

正答：ア
**翻译**：正确答案：ア

---

### 补充关键术语对照表
| 日语原文 | 读音 | 中文翻译 | 英文 |
| :--- | :--- | :--- | :--- |
| インタプリタ | intapurita | 解释器 | Interpreter |
| 原始プログラム | genshi puroguramu | 源程序 | Source Program |
| 1文（一命令）ずつ | ichibun (ichimeirei) zutsu | 逐句（逐命令） | One statement/command at a time |
| コンパイラ言語 | konpaira gengo | 编译型语言 | Compiled Language |
| コンパイラ | konpaira | 编译器 | Compiler |
| 目的プログラム | mokuteki puroguramu | 目标程序 | Object Program |
| リンカ | rinka | 链接器 | Linker |
| 実行可能なプログラム | jikkou kanou na puroguramu | 可执行程序 | Executable Program |
| ローダ | rōda | 加载器 | Loader |
| 主記憶装置 | shu kioku souchi | 主存储器（内存） | Main Memory |
| トランスレータ | toransurēta | 翻译器 | Translator |

---


别慌，这道题是电源效率的基础计算题，我用大白话一步步给你讲明白👇

---

## 一、先把题目翻译+拆成大白话
原文：
> 定格出力電力500Wで効率80%の電源ユニットがある。この電源ユニットから500Wの出力電力を得るのに最低限必要な入力電力の大きさは何Wか。

翻译：
> 有一个**额定输出功率500W、效率80%**的电源。要从这个电源里得到500W的输出功率，最少需要多少输入功率？

---

## 二、核心概念：电源效率（効率）
电源的效率，就是它把输入的电能，转换成输出电能的比例。
公式非常简单：
$$
\eta = \frac{\text{出力電力}}{\text{入力電力}}
$$
- $\eta$（効率）：效率，这里是80% = 0.8
- 出力電力：电源输出给设备的功率，这里是500W
- 入力電力：你要给电源输入的功率，也就是题目要我们求的东西

---

## 三、代入公式计算
我们把公式变形一下，求「入力電力」：
$$
\text{入力電力} = \frac{\text{出力電力}}{\eta}
$$

代入题目给的数值：
$$
\text{入力電力} = \frac{500}{0.8} = 625 \text{ W}
$$

---

## 四、为什么其他选项不对？
- ア 100W：完全没逻辑，和题目数据无关
- イ 400W：这是用 $500 \times 0.8$ 算出来的，搞反了效率的方向（效率是输出/输入，不是输入×效率=输出的反算）
- ウ 625W：✅ 我们算出来的正确答案
- エ 900W：无中生有的数字，和题目无关

---

## 五、给你一个通用小技巧
以后遇到电源效率题，记住：
> 要得到「输出功率」，输入功率一定比输出功率大，因为电源工作时会发热、损耗电能。
> 所以：
> 入力電力 = 出力電力 ÷ 効率（効率要转成小数，比如80%→0.8）

---

✅ 正确答案是 **C（ウ：625W）**


電源ユニット（でんげんユニット /dengen yunitto）
中文：电源单元 / 电源模块 / 电源供应器
英文：Power Supply Unit，缩写 PSU

# **数据中心里给服务器、网络设备用「直流供电」，它的优势是什么？**

## 一、题目翻译
原文：
> データセンタなどで採用されているサーバ，ネットワーク機器に対する直流給電の利点として，適切なものはどれか。

翻译：
> 下列哪一项，是数据中心等场所中，为服务器、网络设备采用**直流供电**的优势？

## 二、核心背景先搞懂
数据中心的设备，本质上都用**直流电（DC）**工作（CPU、内存、主板都靠低压直流运行）。
传统的供电流程是：
`电网交流电(AC) → 设备电源 → 直流电(DC)`
这个过程里，AC→DC的转换会产生大量损耗和热量。

而直流供电的流程是：
`电网交流电(AC) → 中心直流电源 → 设备（直接用DC）`
这样就省去了设备内部的AC→DC转换环节，减少了损耗。

---

## 三、选项翻译+解析
| 选项 | 日语原文 | 中文翻译 | 解析 |
| :--- | :--- | :--- | :--- |
| ア | 交流から直流への変換，直流から交流への変換で生じる電力損失を低減できる。 | 可以减少交流→直流、直流→交流转换过程中产生的电力损耗。 | ✅ **正确**。这正是直流供电的核心优势：减少了多次AC/DC转换，降低了损耗，提高了能源效率。 |
| イ | 受電設備からCPUなどのLSIまで，同じ電圧のまま給電できる。 | 可以从受电设备到CPU等LSI芯片，都以同一电压供电。 | ❌ 错误。数据中心的直流供电电压（比如-48V）和CPU用的低压直流（比如12V、5V、3.3V）完全不同，设备内部还是需要降压，电压不可能全程一样。 |
| ウ | 停電の危険がないので，電源バックアップ用のバッテリを不要にできる。 | 没有停电的风险，因此可以省去电源备份用的电池。 | ❌ 错误。直流供电只是供电方式，不会消除停电风险，数据中心依然需要UPS或电池备份来应对电网断电。 |
| エ | トランスを用いて容易に昇圧，降圧ができる。 | 可以用变压器轻松地升压、降压。 | ❌ 错误。变压器（トランス）只能对交流电（AC）进行升压/降压，直流电无法直接用变压器变压。 |

## 四、结论
✅ 正确答案是 **A（ア）**，直流供电的核心优势就是减少AC/DC转换带来的电力损耗，提高能源效率。

💡 补充小知识：
数据中心常用的直流供电，通常是-48V直流，它的好处还有：
1.  安全性更高（电压低，触电风险小）
2.  设备电源结构更简单，故障率更低
3.  可以直接和电池组并联，实现更高效的不间断供电




我帮你直接按题目解析，把四个选项对应的知识点总结清楚👇

---
OSやアプリケーションのバグなどが原因で，動作中に確保した主記憶が解放されないことであり，これが発生すると主記憶中の利用可能な部分が減少する。
### ア：メモリリーク（内存泄漏 / Memory Leak）
- **核心定义**：程序申请了内存后，因bug等原因不再使用时**没有释放**，导致系统可用内存持续减少。
- **后果**：响应变慢、程序卡死，严重时导致系统崩溃。
- **典型场景**：长时间运行的服务器程序、循环中反复申请内存却不释放的代码。

---
アプリケーションの同時実行数を増やした場合に，主記憶容量が不足し，処理時間のほとんどがページングに費やされ，スループットの極端な低下を招くことである。

ページング（paging）
读音：ぺーじんぐ（pējingu）
中文：分页（存储）
英文：Paging
通俗解释
它是操作系统里的一种内存管理技术

原形：費やす（ついやす /tsuiyasu）
中文：被消耗、被花费、被用掉

スループット
读音：するーぷっと（surūputto）
中文：吞吐量
英文：Throughput
通俗解释
在计算机 / 系统里，它指的是单位时间内系统能处理的任务量、数据量

处理时间的大部分都被分页交换消耗掉，导致系统吞吐量急剧下降。

### イ：スラッシング（抖动/颠簸 / Thrashing）
- **核心定义**：系统大部分时间都花在**分页（ページング）**交换上，几乎没有时间执行实际任务，导致吞吐量急剧下降。
- **触发原因**：
  1.  同时运行的程序太多，内存不够用
  2.  单个程序的内存需求远超物理内存容量
- **后果**：系统极度卡顿，几乎无法响应。

---
実行時のプログラム領域の大きさに制限があるときに，必要になったモジュールを主記憶に取り込む手法である。
### ウ：オーバレイ（覆盖技术 / Overlay）
- **核心定义**：当程序运行时的内存空间有限时，只把当前需要的模块加载到内存，不需要的模块暂时不加载，以此解决“程序太大、内存不够”的问题。
- **用途**：早期内存很小时的程序加载技术，现在已很少直接使用。

---
主記憶で利用可能な空き領域の総量は足りているのに，主記憶中に不連続で散在しているので，大きなプログラムをロードする領域が確保できないことである。
### エ：メモリフラグメンテーション（内存碎片 / Memory Fragmentation）
- **核心定义**：系统的空闲内存总量足够，但这些空间分散成了不连续的小块，无法分配给需要大块连续内存的程序。
- **解决方法**：内存压缩（メモリコンパクション / デフラグ），把分散的空闲空间整理成连续的大块。



### 一、LFU 是什么？
LFU 是 **Least Frequently Used** 的缩写，中文叫「**最少使用频率算法**」。

它和 LRU（最近最少使用）一样，都是操作系统里的「页面置换算法」，用来解决内存不够用时，该把哪个页面“踢出去”的问题。

---

### 二、LFU 的核心规则
**谁被访问的次数最少，就把谁换掉。**

举个生活例子：
你书包里只能装3本书，现在来了第4本新的，你必须扔掉一本。
- LFU 会看：这3本书里，哪一本你**翻开的次数最少**，就把它扔掉。

---

### 三、和 LRU 的关键区别
| 算法 | 全称 | 核心判断依据 | 特点 |
| :--- | :--- | :--- | :--- |
| **LRU** | Least **Recently** Used | **最后一次访问的时间**（最近最久没用的） | 关心「最近」的访问时间，不看次数 |
| **LFU** | Least **Frequently** Used | **访问的总次数**（用得最少的） | 关心「频率」，不看什么时候用的 |

---

### 四、LFU 的优缺点
- ✅ **优点**：对“冷门”数据淘汰效率高，适合长期稳定的访问模式。
- ❌ **缺点**：
  1.  实现起来比 LRU 复杂，需要给每个页面维护一个计数器。
  2.  有「冷启动问题」：刚进来的新页面，访问次数是0，很容易被误杀。
  3.  对“偶尔爆发访问”的页面不友好，比如一个页面以前用得少，但最近突然被频繁访问，它的计数器不会立刻跟上。

---

### 五、和题目里的选项对应上了吗？
你题目里的：
- ウ：単位時間当たりの参照頻度（单位时间内的访问频率）
- エ：累積の参照回数（累计访问次数）

这两个都是 LFU 的判断依据，和 LRU 完全无关，所以当时的正确答案是 ア（最后一次访问时刻）。









